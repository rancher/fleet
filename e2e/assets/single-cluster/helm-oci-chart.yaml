kind: GitRepo
apiVersion: fleet.cattle.io/v1alpha1
metadata:
  name: helm-oci-chart
spec:
  repo: https://github.com/p-se/fleet-test-data
  paths:
  - helm-oci-chart
  targets:
  - clusterSelector:
      matchExpressions:
      - key: provider.cattle.io
        operator: NotIn
        values:
        - harvester
